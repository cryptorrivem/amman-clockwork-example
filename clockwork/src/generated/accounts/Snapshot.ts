/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as beet from '@metaplex-foundation/beet'
import * as web3 from '@solana/web3.js'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link Snapshot}
 * @category Accounts
 * @category generated
 */
export type SnapshotArgs = {
  id: beet.bignum
  totalFrames: beet.bignum
  totalStake: beet.bignum
}

export const snapshotDiscriminator = [137, 213, 28, 133, 224, 161, 48, 108]
/**
 * Holds the data for the {@link Snapshot} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class Snapshot implements SnapshotArgs {
  private constructor(
    readonly id: beet.bignum,
    readonly totalFrames: beet.bignum,
    readonly totalStake: beet.bignum
  ) {}

  /**
   * Creates a {@link Snapshot} instance from the provided args.
   */
  static fromArgs(args: SnapshotArgs) {
    return new Snapshot(args.id, args.totalFrames, args.totalStake)
  }

  /**
   * Deserializes the {@link Snapshot} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [Snapshot, number] {
    return Snapshot.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Snapshot} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<Snapshot> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find Snapshot account at ${address}`)
    }
    return Snapshot.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'F8dKseqmBoAkHx3c58Lmb9TgJv5qeTf3BbtZZSEzYvUa'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, snapshotBeet)
  }

  /**
   * Deserializes the {@link Snapshot} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [Snapshot, number] {
    return snapshotBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link Snapshot} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return snapshotBeet.serialize({
      accountDiscriminator: snapshotDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Snapshot}
   */
  static get byteSize() {
    return snapshotBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Snapshot} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      Snapshot.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link Snapshot} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === Snapshot.byteSize
  }

  /**
   * Returns a readable version of {@link Snapshot} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      id: (() => {
        const x = <{ toNumber: () => number }>this.id
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      totalFrames: (() => {
        const x = <{ toNumber: () => number }>this.totalFrames
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      totalStake: (() => {
        const x = <{ toNumber: () => number }>this.totalStake
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const snapshotBeet = new beet.BeetStruct<
  Snapshot,
  SnapshotArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['id', beet.u64],
    ['totalFrames', beet.u64],
    ['totalStake', beet.u64],
  ],
  Snapshot.fromArgs,
  'Snapshot'
)
