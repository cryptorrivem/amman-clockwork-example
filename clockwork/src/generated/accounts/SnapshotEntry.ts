/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link SnapshotEntry}
 * @category Accounts
 * @category generated
 */
export type SnapshotEntryArgs = {
  delegation: web3.PublicKey
  id: beet.bignum
  snapshotFrame: web3.PublicKey
  stakeAmount: beet.bignum
}

export const snapshotEntryDiscriminator = [157, 11, 94, 32, 220, 197, 73, 87]
/**
 * Holds the data for the {@link SnapshotEntry} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class SnapshotEntry implements SnapshotEntryArgs {
  private constructor(
    readonly delegation: web3.PublicKey,
    readonly id: beet.bignum,
    readonly snapshotFrame: web3.PublicKey,
    readonly stakeAmount: beet.bignum
  ) {}

  /**
   * Creates a {@link SnapshotEntry} instance from the provided args.
   */
  static fromArgs(args: SnapshotEntryArgs) {
    return new SnapshotEntry(
      args.delegation,
      args.id,
      args.snapshotFrame,
      args.stakeAmount
    )
  }

  /**
   * Deserializes the {@link SnapshotEntry} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [SnapshotEntry, number] {
    return SnapshotEntry.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link SnapshotEntry} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<SnapshotEntry> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find SnapshotEntry account at ${address}`)
    }
    return SnapshotEntry.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'F8dKseqmBoAkHx3c58Lmb9TgJv5qeTf3BbtZZSEzYvUa'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, snapshotEntryBeet)
  }

  /**
   * Deserializes the {@link SnapshotEntry} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [SnapshotEntry, number] {
    return snapshotEntryBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link SnapshotEntry} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return snapshotEntryBeet.serialize({
      accountDiscriminator: snapshotEntryDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link SnapshotEntry}
   */
  static get byteSize() {
    return snapshotEntryBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link SnapshotEntry} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      SnapshotEntry.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link SnapshotEntry} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === SnapshotEntry.byteSize
  }

  /**
   * Returns a readable version of {@link SnapshotEntry} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      delegation: this.delegation.toBase58(),
      id: (() => {
        const x = <{ toNumber: () => number }>this.id
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      snapshotFrame: this.snapshotFrame.toBase58(),
      stakeAmount: (() => {
        const x = <{ toNumber: () => number }>this.stakeAmount
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const snapshotEntryBeet = new beet.BeetStruct<
  SnapshotEntry,
  SnapshotEntryArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['delegation', beetSolana.publicKey],
    ['id', beet.u64],
    ['snapshotFrame', beetSolana.publicKey],
    ['stakeAmount', beet.u64],
  ],
  SnapshotEntry.fromArgs,
  'SnapshotEntry'
)
