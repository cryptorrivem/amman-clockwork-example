/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link Worker}
 * @category Accounts
 * @category generated
 */
export type WorkerArgs = {
  authority: web3.PublicKey
  commissionBalance: beet.bignum
  commissionRate: beet.bignum
  id: beet.bignum
  signatory: web3.PublicKey
  totalDelegations: beet.bignum
}

export const workerDiscriminator = [224, 158, 97, 5, 224, 241, 67, 146]
/**
 * Holds the data for the {@link Worker} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class Worker implements WorkerArgs {
  private constructor(
    readonly authority: web3.PublicKey,
    readonly commissionBalance: beet.bignum,
    readonly commissionRate: beet.bignum,
    readonly id: beet.bignum,
    readonly signatory: web3.PublicKey,
    readonly totalDelegations: beet.bignum
  ) {}

  /**
   * Creates a {@link Worker} instance from the provided args.
   */
  static fromArgs(args: WorkerArgs) {
    return new Worker(
      args.authority,
      args.commissionBalance,
      args.commissionRate,
      args.id,
      args.signatory,
      args.totalDelegations
    )
  }

  /**
   * Deserializes the {@link Worker} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [Worker, number] {
    return Worker.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Worker} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<Worker> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find Worker account at ${address}`)
    }
    return Worker.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'F8dKseqmBoAkHx3c58Lmb9TgJv5qeTf3BbtZZSEzYvUa'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, workerBeet)
  }

  /**
   * Deserializes the {@link Worker} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [Worker, number] {
    return workerBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link Worker} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return workerBeet.serialize({
      accountDiscriminator: workerDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Worker}
   */
  static get byteSize() {
    return workerBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Worker} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      Worker.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link Worker} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === Worker.byteSize
  }

  /**
   * Returns a readable version of {@link Worker} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      authority: this.authority.toBase58(),
      commissionBalance: (() => {
        const x = <{ toNumber: () => number }>this.commissionBalance
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      commissionRate: (() => {
        const x = <{ toNumber: () => number }>this.commissionRate
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      id: (() => {
        const x = <{ toNumber: () => number }>this.id
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      signatory: this.signatory.toBase58(),
      totalDelegations: (() => {
        const x = <{ toNumber: () => number }>this.totalDelegations
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const workerBeet = new beet.BeetStruct<
  Worker,
  WorkerArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['authority', beetSolana.publicKey],
    ['commissionBalance', beet.u64],
    ['commissionRate', beet.u64],
    ['id', beet.u64],
    ['signatory', beetSolana.publicKey],
    ['totalDelegations', beet.u64],
  ],
  Worker.fromArgs,
  'Worker'
)
